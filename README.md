# KAJA

Logo-led, multilingual product presentation built with Next.js, TypeScript, and Tailwind CSS.

## Commands

```bash
pnpm dev
pnpm lint
pnpm typecheck
pnpm check:loc
pnpm build
```

## Deployment

Pushes to the GitHub `main` branch are the deployment trigger. No Vercel CLI setup is required by this repository.
